import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1
  },
  backImage: {
    position: 'absolute',
    width: '100%',
    bottom: 0,
    height: 600,
    zIndex: -1
  }
});

import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    // borderWidth: 1,
    // borderColor: 'black',
    textAlign: 'center',
    paddingVertical: 40
  },
  title: {
    textAlign: 'center',
    fontSize: 24,
    paddingHorizontal: 10
  }
});

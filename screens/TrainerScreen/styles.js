import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    // backgroundColor: 'black' // whole bg
  },
  backBtn: {
    width: 40,
    alignItems: 'center',
    // borderColor: 'black',
    // borderWidth: 1
  }
});

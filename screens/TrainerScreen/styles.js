import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1
  },
  backBtn: {
    width: 40,
    alignItems: 'center',
    // borderColor: 'black',
    // borderWidth: 1
  }
});
